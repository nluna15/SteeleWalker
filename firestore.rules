rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

    match /dogs/{dogId} {
      allow read, delete, update: if request.auth != null && resource != null && request.auth.uid == resource.data.user_id;
      allow create: if request.auth != null && request.auth.uid == request.resource.data.user_id;
    }

    match /walk_logs/{logId} {
      allow read, delete, update: if request.auth != null && resource != null && request.auth.uid == resource.data.user_id;
      allow create: if request.auth != null && request.auth.uid == request.resource.data.user_id;
    }

    match /scheduled_walks/{walkId} {
      allow read, delete, update: if request.auth != null && resource != null && request.auth.uid == resource.data.user_id;
      allow create: if request.auth != null && request.auth.uid == request.resource.data.user_id;
    }

    match /walking_schedules/{scheduleId} {
      allow read, delete, update: if request.auth != null && resource != null && request.auth.uid == resource.data.user_id;
      allow create: if request.auth != null && request.auth.uid == request.resource.data.user_id;
    }

    match /breeds/{breedId} {
      allow read: if request.auth != null;
      allow write: if false;
    }
  }
}
